jobs:
  - script: >
      pipelineJob('kayobe-run-command') {
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/RSE-Cambridge/cumulus-kayobe-config')
                }
                branch('*/preprod-train-jenkins')
              }
            }
            lightweight()
          }
        }
      }
